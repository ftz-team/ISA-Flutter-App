import 'package:flutter/material.dart';

class liveScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("PlaceHolder for the live screen"),
      ),
    );
  }
}
