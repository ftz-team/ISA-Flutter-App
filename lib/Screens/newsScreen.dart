import 'package:flutter/material.dart';

class newsScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("PlaceHolder for the news screen"),
      ),
    );
  }
}
